# Sign Language Translator - pinlangan dependency'lar
# Python 3.12  |  CUDA 13.0 (RTX 5060 / Blackwell)  |  mediapipe 0.10.x (solutions.holistic)
#
# MUHIM: mediapipe==0.10.21 numpy<2 va protobuf<5 talab qiladi.
# Buning uchun numpy va protobuf shu versiyada PUAN qilingan.

# ---- mediapipe (keypoint extraction, solutions.holistic) ----
mediapipe==0.10.21
numpy==1.26.4
protobuf==4.25.3

# ---- PyTorch (GPU, CUDA 13.0) ----
# O'rnatish tartibi: pip install -r requirements.txt --index-url https://download.pytorch.org/whl/cu130 --extra-index-url https://pypi.org/simple
torch==2.9.1+cu130

# ---- Umumiy ML/data ----
opencv-python
pandas
scipy
scikit-learn
mlflow
tqdm

# ---- realtime / utility ----
pyyaml